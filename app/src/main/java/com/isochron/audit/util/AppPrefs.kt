package com.isochron.audit.util

/** Keys of the app-wide SharedPreferences file. */
object AppPrefs {
    const val NAME = "scanner_prefs"
    const val KEY_ONBOARDING_COMPLETE = "onboarding_complete"
    const val KEY_MONITOR_INTERVAL = "monitor_interval_default"
    const val DEFAULT_MONITOR_INTERVAL = 10
}
